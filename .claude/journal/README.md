# Journal — 세션 로그

Hermes의 session search에 해당. `/reflect`가 `YYYY-MM-DD.md`에 항목을 덧붙이고,
`python3 scripts/recall.py <키워드>`로 검색한다.

항목 형식:
```
## HH:MM — <작업 한 줄 요약>
- 한 일:
- 배운 것:
- 영속화: (MEMORY / USER / skill:<name> / 없음)
```
